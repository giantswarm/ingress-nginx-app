configmap:
  worker-processes: "2"
controller:
  replicaCount: 2
  autoscaling:
    enabled: false
  resources:
    requests:
      cpu: 100m
      memory: 90Mi
  service:
    suffix: "-test"
    type: NodePort
